import React, { Component } from 'react';

class Form extends Component {
    render() {
        return (
            <div>
                <input type="text"/>
                <button type="button">Submit</button>
            </div>
        );
    }
}

export default Form;